<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Мой сайт — шаблон</title>

    <style>
        /* ========================================
           1. ОБЩИЕ НАСТРОЙКИ (СБРОС СТИЛЕЙ)
           ======================================== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box; /* чтобы padding не увеличивал размер блока */
        }

        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            line-height: 1.6;
            color: #2b2b2b;
            background-color: #ffffff;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        ul {
            list-style: none;
        }

        img {
            max-width: 100%;
            display: block;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* ========================================
           2. ШАПКА САЙТА (HEADER)
           ======================================== */
        header {
            position: sticky;
            top: 0;
            background: #ffffff;
            box-shadow: 0 2px 8px rgba(0,0,0,0.06);
            z-index: 100;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 18px 0;
        }

        .logo {
            font-size: 22px;
            font-weight: 700;
            color: #111111;
        }

        .nav-links {
            display: flex;
            gap: 30px;
        }

        .nav-links a {
            font-weight: 500;
            transition: color 0.2s;
        }

        .nav-links a:hover {
            color: #111111;
        }

        /* Кнопка-бургер для мобильной версии */
        .burger {
            display: none;
            flex-direction: column;
            gap: 5px;
            cursor: pointer;
            background: none;
            border: none;
        }

        .burger span {
            width: 26px;
            height: 3px;
            background: #2b2b2b;
            border-radius: 3px;
        }

        /* ========================================
           3. ГЛАВНЫЙ ЭКРАН (HERO)
           ======================================== */
        .hero {
            background: linear-gradient(135deg, #0d0d0d, #2b2b2b);
            color: #fff;
            padding: 100px 0;
            text-align: center;
        }

        .hero h1 {
            font-size: 44px;
            margin-bottom: 20px;
        }

        .hero p {
            font-size: 18px;
            max-width: 600px;
            margin: 0 auto 35px;
            opacity: 0.9;
        }

        .btn {
            display: inline-block;
            padding: 14px 32px;
            background: #ffffff;
            color: #111111;
            font-weight: 600;
            border-radius: 8px;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.15);
        }

        .btn-outline {
            background: transparent;
            border: 2px solid #fff;
            color: #fff;
            margin-left: 15px;
        }

        /* ========================================
           4. СЕКЦИЯ "О НАС" / КАРТОЧКИ
           ======================================== */
        section {
            padding: 80px 0;
        }

        .section-title {
            text-align: center;
            font-size: 32px;
            margin-bottom: 50px;
        }

        .section-title span {
            color: #111111;
            border-bottom: 3px solid #111111;
        }

        .cards {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
        }

        .card {
            background: #f9f9fb;
            border-radius: 14px;
            padding: 35px 25px;
            text-align: center;
            transition: transform 0.25s, box-shadow 0.25s;
        }

        .card:hover {
            transform: translateY(-6px);
            box-shadow: 0 12px 24px rgba(0,0,0,0.08);
        }

        .card-icon {
            width: 48px;
            height: 48px;
            margin: 0 auto 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #111111;
            border-radius: 12px;
        }

        .card-icon svg {
            width: 24px;
            height: 24px;
            stroke: #ffffff;
        }

        .card h3 {
            margin-bottom: 12px;
            font-size: 20px;
        }

        /* ========================================
           5. СЕКЦИЯ С ФОНОМ (например "О компании")
           ======================================== */
        .about {
            background: #f3f2ff;
        }

        .about-content {
            display: flex;
            align-items: center;
            gap: 50px;
        }

        .about-content img {
            border-radius: 16px;
            flex: 1;
        }

        .about-text {
            flex: 1;
        }

        .about-text h2 {
            font-size: 30px;
            margin-bottom: 20px;
        }

        .about-text p {
            margin-bottom: 15px;
            color: #555;
        }

        /* ========================================
           6. ФОРМА ОБРАТНОЙ СВЯЗИ
           ======================================== */
        .contact-form {
            max-width: 600px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            gap: 18px;
        }

        .contact-form input,
        .contact-form textarea {
            padding: 14px 16px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 15px;
            font-family: inherit;
        }

        .contact-form input:focus,
        .contact-form textarea:focus {
            outline: none;
            border-color: #4f46e5;
        }

        .contact-form button {
            padding: 14px;
            background: #4f46e5;
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s;
        }

        .contact-form button:hover {
            background: #4338ca;
        }

        /* ========================================
           7. ПОДВАЛ (FOOTER)
           ======================================== */
        footer {
            background: #1e1b2e;
            color: #cfcfe0;
            padding: 40px 0;
            text-align: center;
        }

        footer .socials {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 15px;
        }

        footer .socials a {
            font-size: 20px;
            transition: color 0.2s;
        }

        footer .socials a:hover {
            color: #ffffff;
        }

        /* ========================================
           8. АДАПТИВНОСТЬ (МОБИЛЬНЫЕ УСТРОЙСТВА)
           ======================================== */
        @media (max-width: 768px) {
            .nav-links {
                position: absolute;
                top: 100%;
                left: 0;
                right: 0;
                background: #fff;
                flex-direction: column;
                align-items: center;
                padding: 20px 0;
                display: none;
                box-shadow: 0 8px 15px rgba(0,0,0,0.08);
            }

            .nav-links.active {
                display: flex;
            }

            .burger {
                display: flex;
            }

            .hero h1 {
                font-size: 30px;
            }

            .cards {
                grid-template-columns: 1fr;
            }

            .about-content {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>

    <!-- ========================================
         ШАПКА
         ======================================== -->
    <header>
        <div class="container navbar">
            <div class="logo">МойСайт</div>

            <ul class="nav-links" id="navLinks">
                <li><a href="#hero">Главная</a></li>
                <li><a href="#services">Услуги</a></li>
                <li><a href="#about">О нас</a></li>
                <li><a href="#contact">Контакты</a></li>
            </ul>

            <!-- Кнопка меню для телефонов -->
            <button class="burger" id="burgerBtn">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
    </header>

    <!-- ========================================
         ГЛАВНЫЙ ЭКРАН
         ======================================== -->
    <section class="hero" id="hero">
        <div class="container">
            <h1>Заголовок вашего сайта</h1>
            <p>Здесь короткое описание того, чем вы занимаетесь и почему это должно заинтересовать посетителя.</p>
            <a href="#contact" class="btn">Связаться</a>
            <a href="#about" class="btn btn-outline">Узнать больше</a>
        </div>
    </section>

    <!-- ========================================
         УСЛУГИ / ПРЕИМУЩЕСТВА
         ======================================== -->
    <section id="services">
        <div class="container">
            <h2 class="section-title">Наши <span> услуги</span></h2>

            <div class="cards">
                <div class="card">
                    <div class="card-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09z"/><path d="M12 15l-3-3a22 22 0 0 1 2-3.95A12.88 12.88 0 0 1 22 2c0 2.72-.78 7.5-6 11a22.35 22.35 0 0 1-4 2z"/><path d="M9 12H4s.55-3.03 2-4c1.62-1.08 5 0 5 0"/><path d="M12 15v5s3.03-.55 4-2c1.08-1.62 0-5 0-5"/></svg>
                    </div>
                    <h3>Быстрый старт</h3>
                    <p>Краткое описание первой услуги или преимущества вашей компании.</p>
                </div>

                <div class="card">
                    <div class="card-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="13.5" cy="6.5" r=".5"/><circle cx="17.5" cy="10.5" r=".5"/><circle cx="8.5" cy="7.5" r=".5"/><circle cx="6.5" cy="12.5" r=".5"/><path d="M12 2C6.5 2 2 6.5 2 12s4.5 10 10 10c.926 0 1.648-.746 1.648-1.688 0-.437-.18-.835-.437-1.125-.29-.289-.438-.652-.438-1.125a1.64 1.64 0 0 1 1.668-1.668h1.996c3.051 0 5.555-2.503 5.555-5.554C21.965 6.012 17.461 2 12 2z"/></svg>
                    </div>
                    <h3>Красивый дизайн</h3>
                    <p>Краткое описание второй услуги или преимущества.</p>
                </div>

                <div class="card">
                    <div class="card-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18h6"/><path d="M10 22h4"/><path d="M12 2a7 7 0 0 0-7 7c0 2.5 1.5 4 2.5 5.5S9 17 9 18h6c0-1 .5-2 1.5-3.5S19 11.5 19 9a7 7 0 0 0-7-7z"/></svg>
                    </div>
                    <h3>Идеи и решения</h3>
                    <p>Краткое описание третьей услуги или преимущества.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- ========================================
         О НАС
         ======================================== -->
    <section class="about" id="about">
        <div class="container about-content">
            <img src="https://placehold.co/500x350?text=Ваше+фото" alt="О компании">

            <div class="about-text">
                <h2>О нас</h2>
                <p>Расскажите здесь историю вашей компании, проекта или лично о себе. Этот текст должен вызывать доверие у посетителя.</p>
                <p>Добавьте пару предложений о вашей миссии или ценностях.</p>
                <a href="#contact" class="btn" style="background:#4f46e5; color:#fff;">Связаться с нами</a>
            </div>
        </div>
    </section>

    <!-- ========================================
         КОНТАКТЫ / ФОРМА
         ======================================== -->
    <section id="contact">
        <div class="container">
            <h2 class="section-title">Свяжитесь <span>с нами</span></h2>

            <form class="contact-form" onsubmit="handleSubmit(event)">
                <input type="text" placeholder="Ваше имя" required>
                <input type="email" placeholder="Ваш email" required>
                <textarea rows="5" placeholder="Ваше сообщение" required></textarea>
                <button type="submit">Отправить</button>
            </form>
        </div>
    </section>

    <!-- ========================================
         ПОДВАЛ
         ======================================== -->
    <footer>
        <div class="container">
            <div class="socials">
                <a href="#">VK</a>
                <a href="#">Telegram</a>
                <a href="#">Instagram</a>
            </div>
            <p>&copy; 2026 МойСайт. Все права защищены.</p>
        </div>
    </footer>

    <script>
        // Открытие/закрытие мобильного меню
        const burgerBtn = document.getElementById('burgerBtn');
        const navLinks = document.getElementById('navLinks');

        burgerBtn.addEventListener('click', () => {
            navLinks.classList.toggle('active');
        });

        // Закрываем меню при клике на ссылку (для мобильных)
        document.querySelectorAll('.nav-links a').forEach(link => {
            link.addEventListener('click', () => {
                navLinks.classList.remove('active');
            });
        });

        // Плавная прокрутка к разделам
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            });
        });

        // Обработка отправки формы (заглушка — замените на реальную отправку)
        function handleSubmit(event) {
            event.preventDefault();
            alert('Спасибо! Ваше сообщение отправлено (демо-режим).');
            event.target.reset();
        }
    </script>

</body>
</html>
